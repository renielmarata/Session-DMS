const createAccessToken = (data) => {
    try {
        console.log("create accessToken utils");
    } catch (err) {
        console.log(err);
        return false;
    }
}

module.exports = createAccessToken;